### 逻辑填空记录统计表

```dataviewjs
// 全局补丁：将 mousewheel/wheel 监听器默认设为 passive，避免滚动阻塞
(function enforcePassiveWheel() {
  if (window.__passiveWheelPatched__) return;
  const wheelEvents = ['wheel', 'mousewheel', 'DOMMouseScroll'];
  const origAdd = EventTarget.prototype.addEventListener;
  EventTarget.prototype.addEventListener = function(type, listener, options) {
    if (wheelEvents.includes(type)) {
      if (options === undefined) {
        options = { passive: true };
      } else if (typeof options === 'boolean') {
        options = { capture: options, passive: true };
      } else if (options && typeof options === 'object' && options.passive === undefined) {
        options = Object.assign({}, options, { passive: true });
      }
    }
    return origAdd.call(this, type, listener, options);
  };
  window.__passiveWheelPatched__ = true;
})();

// 统一工具库：EChartsKit（跨本页所有 dataviewjs 复用，合并注入 + polyfill）
(function initEChartsKit(){
  const EC = window.EChartsKit || {};

  // 1) 环境检测
  if (!EC.isMobile) {
    EC.isMobile = () => /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) || window.innerWidth <= 768;
  }

  // 2) Dataview 容器通用样式
  if (!EC.applyDvContainerDefaults) {
    EC.applyDvContainerDefaults = (dv) => {
      dv.container.style.width = '100%';
      dv.container.style.maxWidth = 'none';
      dv.container.style.overflow = 'visible';
      dv.container.style.padding = '0';
      dv.container.style.margin = '0';
    };
  }

  // 3) 实际宽度计算：统一 getActualWidth 与 computeActualWidth 两种命名
  const defaultComputeActualWidth = (dvContainer) => {
    const isMobile = EC.isMobile();
    if (isMobile) {
      // 移动端不再人为减去左右边距，直接使用容器/窗口的可用宽度
      const base = (dvContainer && dvContainer.offsetWidth) ? dvContainer.offsetWidth : (window.innerWidth || 800);
      return Math.max(280, Math.min(base, 1200));
    } else {
      const parentWidth = (dvContainer && dvContainer.offsetWidth) ? dvContainer.offsetWidth : 800;
      return Math.min(parentWidth, 1200);
    }
  };
  if (!EC.computeActualWidth && !EC.getActualWidth) {
    EC.computeActualWidth = defaultComputeActualWidth;
  }
  if (!EC.getActualWidth) {
    EC.getActualWidth = (dvContainer) => (EC.computeActualWidth ? EC.computeActualWidth(dvContainer) : defaultComputeActualWidth(dvContainer));
  }
  if (!EC.computeActualWidth) {
    EC.computeActualWidth = (dvContainer) => (EC.getActualWidth ? EC.getActualWidth(dvContainer) : defaultComputeActualWidth(dvContainer));
  }

  // 4) 容器初始化
  if (!EC.initContainer) {
    EC.initContainer = (dv, heightMobile = 300, heightDesktop = 400, marginDesktop = '20px auto', marginMobile = '20px 0 20px 0') => {
      EC.applyDvContainerDefaults(dv);
      const container = document.createElement('div');
      const isMobile = EC.isMobile();
      const actualWidth = EC.computeActualWidth(dv.container);
      container.style.width = actualWidth + 'px';
      container.style.minWidth = actualWidth + 'px';
      container.style.height = (isMobile ? heightMobile : heightDesktop) + 'px';
      container.style.margin = isMobile ? marginMobile : marginDesktop;
      container.style.overflow = 'visible';
      container.style.position = 'relative';
      container.style.zIndex = '1';
      dv.container.appendChild(container);
      return container;
    };
  }

  // 5) Resize 监听：统一为 passive
  if (!EC.addPassiveResize) {
    EC.addPassiveResize = (dv, container, updateHeightFn) => {
      const onResize = () => {
        const isMobile = EC.isMobile();
        const actualWidth = EC.computeActualWidth(dv.container);
        container.style.width = actualWidth + 'px';
        container.style.minWidth = actualWidth + 'px';
        if (typeof updateHeightFn === 'function') {
          container.style.height = updateHeightFn(isMobile) + 'px';
        }
        EC.applyDvContainerDefaults(dv);
        if (container.myChart && container.myChart.resize) container.myChart.resize();
      };
      window.addEventListener('resize', onResize, { passive: true });
    };
  }

  // 6) 图例
  if (!EC.legend) {
    EC.legend = (top = 38, left = 'center', itemGap = 12) => ({ top, left, itemGap, icon: 'circle', type: 'scroll' });
  }

  // 7) 标题（保留兼容，推荐用 insertExplanation 代替）
  if (!EC.title) {
    EC.title = (text, subtext = '', top = 20) => ({
      text,
      subtext,
      top,
      left: 'center',
      textStyle: { fontSize: 16, fontWeight: 'bold' },
      subtextStyle: { fontSize: 12, color: '#888', lineHeight: 20, padding: [2, 0, 0, 0] }
    });
  }

  // 8) 网格：统一 containLabel:true；默认左侧留白最小化（移动端≈8px，桌面端≈max(12px, 视口2%)）
  if (!EC.grid) {
    EC.grid = (cfg = {}) => {
      const isM = EC.isMobile();
      const vw = Math.min(document.body?.clientWidth || window.innerWidth || 800, 1200);
      const minLeft = isM ? 8 : Math.max(12, Math.round(vw * 0.02));
      const base = { top: 56, left: minLeft, right: isM ? 20 : 28, bottom: 24, containLabel: true };
      const merged = Object.assign(base, cfg || {});
      if (typeof merged.left === 'number') merged.left = Math.min(merged.left, minLeft);
      return merged;
    };
  } else {
    const prevGrid = EC.grid.bind(EC);
    EC.grid = (cfg = {}) => {
      const g = prevGrid(cfg) || {};
      if (g.containLabel === undefined) g.containLabel = true;
      const isM = EC.isMobile();
      const vw = Math.min(document.body?.clientWidth || window.innerWidth || 800, 1200);
      const minLeft = isM ? 8 : Math.max(12, Math.round(vw * 0.02));
      if (typeof g.left === 'number') g.left = Math.min(g.left, minLeft);
      return g;
    };
  }

  // 9) 竖向顺序计算统一
  if (!EC.legendRowHeight) EC.legendRowHeight = () => (EC.isMobile() ? 22 : 24);
  if (!EC.nameGap) EC.nameGap = () => (EC.isMobile() ? 10 : 12);
  if (!EC.computeGridTop) {
    EC.computeGridTop = (legendTopPx, extra = 8) => {
      const top = (legendTopPx ?? 38) + EC.legendRowHeight() + EC.nameGap() + (extra ?? 8);
      return top;
    };
  }

  // 10) 左侧像素留白：统一最小化压缩（忽略位数，直接给最小值）
  EC.computeGridLeftForYAxis = function(...args) {
    const isM = EC.isMobile();
    const chartWidthPx = typeof args[3] === 'number'
      ? args[3]
      : Math.min(document.body?.clientWidth || window.innerWidth || 800, 1200);
    const minLeft = isM ? 8 : Math.max(12, Math.round(chartWidthPx * 0.02));
    return minLeft;
  };

  // 11) 插入说明块：遵循“说明→图例→轴名→图”
  if (!EC.insertExplanation) {
    EC.insertExplanation = (dv, textOrNode, options = {}) => {
      const old = dv.container.querySelector(':scope > .echarts-explain');
      if (old) old.remove();
      const wrap = dv.container.createEl('div', { cls: 'echarts-explain' });
      const isM = EC.isMobile();
      const mb = options.marginBottomPx != null ? options.marginBottomPx : (isM ? 6 : 8);
      wrap.style.fontSize = isM ? '12px' : '12px';
      wrap.style.color = '#666';
      wrap.style.lineHeight = '1.5';
      wrap.style.margin = isM ? `0 20px ${mb}px 20px` : `0 0 ${mb}px 0`;
      if (typeof textOrNode === 'string') {
        wrap.textContent = textOrNode;
      } else if (textOrNode instanceof Node) {
        wrap.appendChild(textOrNode);
      }
      return wrap;
    };
  }

  // 12) 数值与货币格式
  if (!EC.formatShortNumber) {
    EC.formatShortNumber = (num, decimals = 1) => {
      if (num === null || num === undefined || isNaN(num)) return '';
      const abs = Math.abs(num);
      if (abs >= 1_000_000) return (num / 1_000_000).toFixed(decimals) + 'm';
      if (abs >= 1_000) return (num / 1_000).toFixed(decimals) + 'k';
      return String(num);
    };
  }
  if (!EC.formatMoney) {
    EC.formatMoney = (num, decimals = 1) => '¥' + EC.formatShortNumber(num, decimals);
  }
  if (!EC.colors) EC.colors = ['#5470c6','#91cc75','#fac858','#ee6666','#73c0de','#3ba272','#fc8452','#9a60b4','#ea7ccc'];

  // 合并而非覆盖，避免跨页打开后重置全局工具库导致版式回退
  window.EChartsKit = Object.assign(window.EChartsKit || {}, EC);
})();

// 若未加载ECharts，则加载本地库
if (!window.echarts) {
  const echartsFile = app.vault.getAbstractFileByPath('08-科技树系统/01-OB_LifeOS_Build/00-通用模块库/echarts.js');
  if (echartsFile) {
    app.vault.read(echartsFile).then(content => {
      const script = document.createElement('script');
      script.textContent = content;
      document.head.appendChild(script);
      
      // 设置ECharts全局配置以优化事件处理
      if (window.echarts) {
        echarts.registerPostInit(function(chart) {
          // 尝试优化图表的事件处理，减少非被动事件监听器的使用
          chart.getZr().handler.__mouseup = function(event) {
            // 重写鼠标事件处理，避免阻塞滚动
            return true;
          };
        });
      }
      
      createRecordTableChart();
    });
  } else {
    dv.paragraph('错误：未找到本地ECharts库');
  }
} else {
  // 如果ECharts已加载，也设置全局配置
  echarts.registerPostInit(function(chart) {
    // 尝试优化图表的事件处理，减少非被动事件监听器的使用
    chart.getZr().handler.__mouseup = function(event) {
      // 重写鼠标事件处理，避免阻塞滚动
      return true;
    };
  });
  
  createRecordTableChart();
}

function createRecordTableChart() {
  // 读取原始数据
  const originalFile = app.vault.getAbstractFileByPath("07-项目系统/01-公务员考试/01-行测/01-刷题复盘/06-刷题记录/判断推理记录.md");
  if (!originalFile) {
    dv.paragraph('错误：无法找到原始数据文件');
    return;
  }

  app.vault.read(originalFile).then(content => {
    // 解析行数据
    const records = content.split('\n').filter(line => line.includes('【') && line.includes('】'));
    const data = records.map(record => {
      const match = record.match(/【(\d{4})(\d{2})(\d{2})-\d{2}-(.+?)】(\d+)分钟，完成(\d+)道题，答对(\d+)道，答错(\d+)道.*正确率([\d.]+)%/);
      if (!match) return null;
      return {
        date: `${match[1]}-${match[2]}-${match[3]}`,
        examType: match[4],
        time: parseInt(match[5]),
        total: parseInt(match[6]),
        correct: parseInt(match[7]),
        wrong: parseInt(match[8]),
        accuracy: parseFloat(match[9])
      };
    }).filter(Boolean);

    // 按日期汇总
    const dateMap = {};
    data.forEach(item => {
      if (!dateMap[item.date]) {
        dateMap[item.date] = { total: 0, correct: 0, time: 0, records: [] };
      }
      dateMap[item.date].total += item.total;
      dateMap[item.date].correct += item.correct;
      dateMap[item.date].time += item.time;
      dateMap[item.date].records.push(item);
    });

    const rows = Object.entries(dateMap).map(([date, g]) => ({
      date,
      questionCount: g.total,
      accuracy: +(g.correct / g.total * 100).toFixed(1),
      totalTime: g.time
    })).sort((a, b) => new Date(a.date) - new Date(b.date));

    // 计算最大值用于归一化
    const maxQ = Math.max(...rows.map(r => r.questionCount));
    const maxT = Math.max(...rows.map(r => r.totalTime));

    // 容器及自适应设置
    dv.container.style.width = '100%';
    dv.container.style.maxWidth = 'none';
    dv.container.style.overflow = 'visible';

    const container = document.createElement('div');
    const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) || window.innerWidth <= 768;
    const getActualWidth = () => {
      if (isMobile) {
        // 手机端：使用屏幕宽度减去边距，并缩小10px避免横向滚动
        return Math.min(window.innerWidth - 30, window.innerWidth * 0.95 - 10);
      } else {
        // 电脑端：使用父容器宽度或固定宽度
        const parentWidth = dv.container.offsetWidth || 800;
        return Math.min(parentWidth, 1200); // 最大宽度1200px
      }
    };
    const actualWidth = getActualWidth();
    // 统一使用手机端的行高以适应垂直布局的条形图
    const rowHeight = 40;
    const baseHeight = 140; // 含标题/图例等
    const chartHeight = baseHeight + rowHeight * rows.length;

    container.style.width = actualWidth + 'px';
    container.style.minWidth = actualWidth + 'px';
    container.style.height = chartHeight + 'px';
    container.style.margin = '16px auto';

    // 表格式三列可视化（日期、刷题量、正确率、用时）
    const table = container; // 复用已创建的容器
    table.innerHTML = '';

    // 外观
    table.style.background = '#fff';
    table.style.border = '1px solid #e5e7eb';
    table.style.borderRadius = '10px';
    table.style.boxShadow = '0 2px 6px rgba(0,0,0,0.06)';
    table.style.padding = '8px'; // 统一使用手机端的内边距

    // 头部标题移除：统一由Markdown标题控制，避免双标题与风格不一致

    // 紧凑总览行（胶囊标签）
    const totalDays = rows.length;
    const totalQuestions = rows.reduce((s, r) => s + r.questionCount, 0);
    const avgAccuracy = +(
      rows.reduce((s, r) => s + r.accuracy, 0) / (totalDays || 1)
    ).toFixed(1);
    const totalTime = rows.reduce((s, r) => s + r.totalTime, 0);
    const avgTimePerDay = +(
      totalTime / (totalDays || 1)
    ).toFixed(1);

    const summaryRow = document.createElement('div');
    summaryRow.style.display = 'flex';
    summaryRow.style.flexWrap = 'wrap';
    summaryRow.style.gap = '6px'; // 统一使用手机端的较小间距
    summaryRow.style.alignItems = 'center';
    summaryRow.style.margin = '2px 4px 6px'; // 统一使用手机端的较小边距

    const makePill = (text, color) => {
      const pill = document.createElement('span');
      pill.style.display = 'inline-flex';
      pill.style.alignItems = 'center';
      pill.style.gap = '4px'; // 统一使用手机端的较小间距
      pill.style.fontSize = '11px'; // 统一使用手机端的字体大小
      pill.style.color = '#374151';
      pill.style.background = '#f9fafb';
      pill.style.border = '1px solid #e5e7eb';
      pill.style.borderRadius = '999px';
      pill.style.padding = '2px 6px'; // 统一使用手机端的较小内边距
      pill.style.lineHeight = '1';
      pill.style.whiteSpace = 'nowrap';
      const dot = document.createElement('span');
      dot.style.display = 'inline-block';
      dot.style.width = '5px'; // 统一使用手机端的较小圆点
      dot.style.height = '5px';
      dot.style.borderRadius = '999px';
      dot.style.background = color;
      pill.appendChild(dot);
      pill.appendChild(document.createTextNode(text));
      return pill;
    };

    summaryRow.appendChild(makePill(`总天数 ${totalDays}`, '#2196F3'));
    summaryRow.appendChild(makePill(`总题量 ${totalQuestions}`, '#4CAF50'));
    summaryRow.appendChild(makePill(`平均正确率 ${avgAccuracy}%`, '#FFC107'));
    summaryRow.appendChild(makePill(`总耗时 ${totalTime}分钟`, '#9C27B0'));
    summaryRow.appendChild(makePill(`日均耗时 ${avgTimePerDay}分钟`, '#FF5722'));

    table.appendChild(summaryRow);

    // 表头
    const header = document.createElement('div');
    const colGapPx = 8; // 统一使用手机端的较小间距
    const dateColWidth = 80; // 统一使用手机端的较窄日期列
    header.style.display = 'grid';
    // 统一使用手机端的列宽比例
    header.style.gridTemplateColumns = `${dateColWidth}px 1.2fr 1fr 1fr`;
    header.style.columnGap = colGapPx + 'px';
    header.style.alignItems = 'center';
    header.style.padding = '6px 4px'; // 统一使用手机端的较小内边距
    header.style.borderBottom = '1px solid #f1f5f9';
    const hcells = ['日期','刷题量','正确率','用时'].map(txt => {
      const d = document.createElement('div');
      d.textContent = txt;
      d.style.fontSize = '11px'; // 统一使用手机端的较小字体
      d.style.color = '#6b7280';
      d.style.fontWeight = '600';
      d.style.textAlign = 'center'; // 表头在各自列内居中
      return d;
    });
    hcells.forEach(c => header.appendChild(c));
    table.appendChild(header);

    // 行构造函数：带条形可视化
    const makeBarCell = (value, max, color, suffix) => {
      const wrap = document.createElement('div');
      // 统一使用垂直布局，数据在条形图下方
      wrap.style.display = 'flex';
      wrap.style.flexDirection = 'column';
      wrap.style.alignItems = 'center';
      wrap.style.justifyContent = 'center'; // 整体在该列中居中
      wrap.style.gap = '2px'; // 统一使用手机端的较小间距
      wrap.style.width = '100%';

      // 根据表宽动态计算本列可用宽度，避免溢出
      const tablePadding = 8; // 统一使用手机端的内边距
      // 统一使用手机端的列宽比例计算
      const dateColRatio = dateColWidth;
      const dataColRatio = 1.2; // 统一使用手机端第一数据列占更多空间
      const totalRatio = dateColRatio + dataColRatio + 1 + 1; // 总比例
      const availableWidth = (actualWidth - 2 * tablePadding - (3 * colGapPx)) / totalRatio; // 按比例分配
      const labelMin = 45; // 统一使用手机端的标签最小宽度
      const innerGap = 2; // 统一使用手机端的内部间距
      const maxBarPx = 80; // 统一使用手机端的条形图最大宽度
      const barPx = Math.max(60, Math.min(maxBarPx, availableWidth * dataColRatio - labelMin - innerGap));

      const barBg = document.createElement('div');
      barBg.style.flex = 'none';
      barBg.style.width = barPx + 'px';
      barBg.style.height = '8px'; // 统一使用手机端的条形图高度
      barBg.style.background = '#f1f5f9';
      barBg.style.borderRadius = '999px';
      barBg.style.overflow = 'hidden';

      const barFill = document.createElement('div');
      const pct = max > 0 ? Math.min(100, Math.round(value / max * 100)) : 0;
      barFill.style.width = pct + '%';
      barFill.style.height = '100%';
      barFill.style.background = color;
      barFill.style.borderRadius = '999px';

      barBg.appendChild(barFill);

      const label = document.createElement('div');
      label.textContent = `${value}${suffix || ''}`;
      label.style.minWidth = labelMin + 'px';
      label.style.textAlign = 'center'; // 数字居中显示
      label.style.fontSize = '11px'; // 统一使用手机端的字体大小
      label.style.color = '#374151';
      label.style.whiteSpace = 'nowrap';

      // 统一先添加条形图，再添加标签（数据在下方）
      wrap.appendChild(barBg);
      wrap.appendChild(label);
      return wrap;
    };

    // 数据行
    rows.forEach((r, i) => {
      const row = document.createElement('div');
      row.style.display = 'grid';
      // 与表头保持一致的网格布局
      // 统一使用手机端的行高以适应垂直布局的条形图
      row.style.gridTemplateColumns = `${dateColWidth}px 1.2fr 1fr 1fr`;
      row.style.columnGap = colGapPx + 'px';
      row.style.alignItems = 'center';
      // 统一使用手机端的行高以适应垂直布局的条形图
      row.style.padding = '10px 4px';
      row.style.background = i % 2 ? '#fafafa' : 'transparent';

      const dateCell = document.createElement('div');
      // 恢复完整日期显示
      dateCell.textContent = r.date;
      dateCell.style.fontSize = '12px'; // 统一使用手机端的字体大小
      dateCell.style.color = '#374151';
      dateCell.style.textAlign = 'center';

      const qCell = makeBarCell(r.questionCount, maxQ, '#2196F3', '题');
      const accColor = r.accuracy >= 80 ? '#22c55e' : (r.accuracy >= 60 ? '#f59e0b' : '#ef4444');
      const accCell = makeBarCell(r.accuracy, 100, accColor, '%');
      const timeCell = makeBarCell(r.totalTime, maxT, '#9C27B0', '分钟');

      row.appendChild(dateCell);
      row.appendChild(qCell);
      row.appendChild(accCell);
      row.appendChild(timeCell);

      table.appendChild(row);
    });

    // 插入到文档中
    dv.container.appendChild(table);

    // 移除统计摘要：避免信息重复与理解成本，提高首屏聚焦度
  }).catch(err => {
    dv.paragraph(`读取文件错误: ${err.message}`);
  });
}
```

### 正确率趋势图

```dataviewjs
// 检查ECharts是否已加载
if (!window.echarts) {
  // 加载本地ECharts库
  const echartsFile = app.vault.getAbstractFileByPath('08-科技树系统/01-OB_LifeOS_Build/00-通用模块库/echarts.js');
  if (echartsFile) {
    app.vault.read(echartsFile).then(content => {
      const script = document.createElement('script');
      script.textContent = content;
      document.head.appendChild(script);
      
      // 设置ECharts全局配置以优化事件处理
      if (window.echarts) {
        echarts.registerPostInit(function(chart) {
          // 尝试优化图表的事件处理，减少非被动事件监听器的使用
          chart.getZr().handler.__mouseup = function(event) {
            // 重写鼠标事件处理，避免阻塞滚动
            return true;
          };
        });
      }
      
      createAccuracyChart();
    });
  } else {
    console.error('ECharts文件未找到');
  }
} else {
  // 如果ECharts已加载，也设置全局配置
  echarts.registerPostInit(function(chart) {
    // 尝试优化图表的事件处理，减少非被动事件监听器的使用
    chart.getZr().handler.__mouseup = function(event) {
      // 重写鼠标事件处理，避免阻塞滚动
      return true;
    };
  });
  
  createAccuracyChart();
}

function createAccuracyChart() {
  // 获取原始数据
  const originalFile = app.vault.getAbstractFileByPath("07-项目系统/01-公务员考试/01-行测/01-刷题复盘/06-刷题记录/判断推理记录.md");
  
  if (!originalFile) {
    dv.paragraph("错误：无法找到原始数据文件");
    return;
  }
  
  app.vault.read(originalFile).then(content => {
    // 解析数据
    const records = content.split("\n").filter(line => line.includes("【") && line.includes("】"));
    
    const data = records.map(record => {
      const match = record.match(/【(\d{4})(\d{2})(\d{2})-\d{2}-(.+?)】(\d+)分钟，完成(\d+)道题，答对(\d+)道，答错(\d+)道.*正确率([\d.]+)%/);
      if (match) {
        return {
          date: `${match[1]}-${match[2]}-${match[3]}`,
          examType: match[4],
          time: parseInt(match[5]),
          total: parseInt(match[6]),
          correct: parseInt(match[7]),
          wrong: parseInt(match[8]),
          accuracy: parseFloat(match[9])
        };
      }
      return null;
    }).filter(item => item !== null);
    
    // 按日期分组并计算累计平均正确率
    const dateGroups = {};
    const sortedData = [...data].sort((a, b) => new Date(a.date) - new Date(b.date));
    
    let cumulativeTotal = 0;
    let cumulativeCorrect = 0;
    
    sortedData.forEach(item => {
      if (!dateGroups[item.date]) {
        dateGroups[item.date] = {
          dailyTotal: 0,
          dailyCorrect: 0,
          cumulativeTotal: 0,
          cumulativeCorrect: 0,
          records: []
        };
      }
      
      // 累加到总计
      cumulativeTotal += item.total;
      cumulativeCorrect += item.correct;
      
      // 当日数据
      dateGroups[item.date].dailyTotal += item.total;
      dateGroups[item.date].dailyCorrect += item.correct;
      dateGroups[item.date].records.push(item);
      
      // 累计数据（到当天为止的所有数据）
      dateGroups[item.date].cumulativeTotal = cumulativeTotal;
      dateGroups[item.date].cumulativeCorrect = cumulativeCorrect;
    });
    
    // 生成图表数据 - 使用每日正确率，同时计算累计平均值
    const chartData = Object.entries(dateGroups).map(([date, group]) => {
      return {
        date: date,
        accuracy: ((group.dailyCorrect / group.dailyTotal) * 100).toFixed(1),
        records: group.records.length,
        totalQuestions: group.dailyTotal
      };
    }).sort((a, b) => new Date(a.date) - new Date(b.date));
    
    // 计算累计平均值曲线数据
    let cumCorrect = 0;
    let cumTotal = 0;
    const cumulativeAccuracyData = [];
    
    // 按日期排序的数据
    const sortedDateGroups = Object.entries(dateGroups).sort((a, b) => new Date(a[0]) - new Date(b[0]));
    
    for (const [date, group] of sortedDateGroups) {
      cumCorrect += group.dailyCorrect;
      cumTotal += group.dailyTotal;
      cumulativeAccuracyData.push([
        date,
        parseFloat(((cumCorrect / cumTotal) * 100).toFixed(1))
      ]);
    }
    
    // 设置DataviewJS容器样式，确保不受宽度限制
    dv.container.style.width = '100%';
    dv.container.style.maxWidth = 'none';
    dv.container.style.overflow = 'visible';
    dv.container.style.padding = '0';
    dv.container.style.margin = '0';
    
    // 创建图表容器
    const container = document.createElement('div');
    // 检测设备类型并设置不同的宽度策略
    const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) || window.innerWidth <= 768;
    
    // 获取实际可用宽度
    const getActualWidth = () => {
      if (isMobile) {
        // 手机端：使用屏幕宽度减去边距，右侧额外增加10px边距以实现整体居中
        return window.innerWidth - 50; // 减去左侧20px和右侧30px边距
      } else {
        // 电脑端：使用父容器宽度或固定宽度
        const parentWidth = dv.container.offsetWidth || 800;
        return Math.min(parentWidth, 1200); // 最大宽度1200px
      }
    };
    
    const actualWidth = getActualWidth();
    
    // 设置固定像素宽度，避免百分比计算问题
      container.style.width = actualWidth + 'px';
      container.style.height = isMobile ? '300px' : '400px';
      container.style.margin = isMobile ? '20px 30px 20px 20px' : '20px auto'; // 手机端使用不对称边距，电脑端居中显示
      container.style.overflow = 'visible';
    container.style.position = 'relative';
    container.style.zIndex = '1';
    container.style.display = 'block';
    
    // 确保容器在初始化前就设置了正确的宽度
    container.style.minWidth = actualWidth + 'px';
    
    // 说明 → 图例 → 轴名 → 图（在容器上方插入说明）
    window.EChartsKit.insertExplanation(dv, '展示每日正确率及累计平均值曲线');
    // 先插入到文档中，确保DOM已准备好
    dv.container.appendChild(container);
    
    // 等待DOM更新后再初始化图表
    setTimeout(() => {
      // 初始化ECharts实例 - 使用配置优化事件处理
      const myChart = echarts.init(container, null, {
        // 配置渲染器，可能有助于减少非被动事件监听器
        renderer: 'canvas',
        // 启用渐进式渲染
        progressive: 200,
        // 配置阈值，减少事件处理频率
        progressiveThreshold: 1000
      });
      // 将myChart绑定到容器上，以便事件处理函数可以访问
      container.myChart = myChart;
    
    const legendTop = 38;
    const option = {
      legend: Object.assign(window.EChartsKit.legend(legendTop, 'center', 12), { type: 'scroll', data: ['每日正确率', '累计平均值'] }),
      tooltip: {
        trigger: 'axis',
        // 添加移动端tooltip样式配置
        backgroundColor: 'rgba(255, 255, 255, 0.95)',
        borderColor: '#ccc',
        borderWidth: 1,
        textStyle: {
          color: '#333',
          fontSize: 14
        },
        extraCssText: 'box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); border-radius: 6px; padding: 12px; max-width: none;',
        formatter: function(params) {
          // 增加容器宽度和数值宽度，确保数字完整显示
          let result = `<div style="min-width: 180px; max-width: 300px; font-size: 14px; line-height: 1.6; word-wrap: break-word; white-space: normal;">
            <div style="margin-bottom: 6px; font-weight: bold;">${params[0].name}</div>`;
          
          params.forEach(param => {
            if (param.seriesName === '每日正确率') {
              result += `<div>${param.seriesName}: <span style="font-weight: bold; display: inline-block; min-width: 80px; font-size: 16px; color: #5470c6;">${param.value}%</span></div>
                        <div>题目数: <span style="display: inline-block; min-width: 60px;">${chartData[param.dataIndex].totalQuestions}题</span></div>`;
            } else if (param.seriesName === '累计平均值') {
              result += `<div>${param.seriesName}: <span style="font-weight: bold; display: inline-block; min-width: 80px; font-size: 16px; color: #91cc75;">${param.value}%</span></div>`;
            }
          });
          
          result += '</div>';
          return result;
        }
      },
        grid: window.EChartsKit.grid({ top: window.EChartsKit.computeGridTop(legendTop), left: window.EChartsKit.computeGridLeftForYAxis(3), bottom: 60, containLabel: true }),
        xAxis: {
        type: 'category',
        data: chartData.map(item => item.date),
        axisLabel: {
          rotate: 45
        }
      },
      yAxis: {
        type: 'value',
        min: function(value) {
          // 动态计算最小值，取数据最小值的90%作为起始点，但不低于20%
          const dataMin = Math.min(...chartData.map(item => item.accuracy), ...cumulativeAccuracyData.map(item => item[1]));
          return Math.max(Math.floor(dataMin * 0.9), 20);
        },
        max: 100,
        name: '正确率(%)',
        nameLocation: 'end',
        nameGap: window.EChartsKit.nameGap(),
        nameTextStyle: {
          fontSize: 14,
          fontWeight: 'normal'
        },
        axisLabel: {
          formatter: function(value) {
            return value;
          },
          fontSize: 12
        },
        position: 'left'
      },
      dataZoom: [
        { type: 'inside' },
        { type: 'slider', bottom: 16, height: 24 }
      ],
      series: [{
        name: '每日正确率',
        type: 'line',
        data: chartData.map(item => parseFloat(item.accuracy)),
        smooth: true,
        symbol: 'circle',
        symbolSize: 5,
        itemStyle: { color: '#5470c6', borderColor: '#fff', borderWidth: 1.5 },
        lineStyle: { width: 2, color: '#5470c6' },
        areaStyle: { 
          color: {
            type: 'linear',
            x: 0, y: 0, x2: 0, y2: 1,
            colorStops: [
              { offset: 0, color: 'rgba(84, 112, 198, 0.3)' },
              { offset: 1, color: 'rgba(84, 112, 198, 0.05)' }
            ]
          }
        },
        markPoint: {
          data: [
            { type: 'max', name: '最高值' },
            { type: 'min', name: '最低值' }
          ]
        }
      },
      {
        name: '累计平均值',
        type: 'line',
        data: cumulativeAccuracyData.map(item => item[1]),
        smooth: true,
        symbol: 'diamond',
        symbolSize: 4,
        itemStyle: { color: '#91cc75', borderColor: '#fff', borderWidth: 1 },
        lineStyle: { width: 2, color: '#91cc75', type: 'dashed' }
      }]
    };
    
    myChart.setOption(option);
      
      // 响应窗口大小变化 - 使用passive事件监听器提高滚动性能
      window.addEventListener('resize', function() {
      // 重新计算实际宽度
      const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) || window.innerWidth <= 768;
      
      const getActualWidth = () => {
        if (isMobile) {
          // 手机端：使用屏幕宽度减去边距，右侧额外增加10px边距以实现整体居中
          return window.innerWidth - 50; // 减去左侧20px和右侧30px边距
        } else {
          // 电脑端：使用父容器宽度或固定宽度
          const parentWidth = dv.container.offsetWidth || 800;
          return Math.min(parentWidth, 1200); // 最大宽度1200px
        }
      };
      
      const actualWidth = getActualWidth();
      
      // 更新容器宽度
      container.style.width = actualWidth + 'px';
      container.style.minWidth = actualWidth + 'px';
      container.style.height = isMobile ? '450px' : '550px'; // 更新高度以适应新图表
      container.style.margin = isMobile ? '20px 30px 20px 20px' : '20px auto'; // 手机端使用不对称边距，电脑端居中显示
      
      // 确保DataviewJS容器样式正确
      dv.container.style.width = '100%';
      dv.container.style.maxWidth = 'none';
      dv.container.style.overflow = 'visible';
      
      // 调整图表大小 - 使用存储在容器上的myChart变量
      if (container.myChart) {
        container.myChart.resize();
      }
    }, { passive: true }); // 标记为passive事件监听器
    }, 100); // 给DOM一点时间来更新
  }).catch(err => {
    dv.paragraph(`读取文件错误: ${err.message}`);
  });
}
```

### 考试类型分布图

```dataviewjs
// 确保ECharts已加载
if (!window.echarts) {
  const echartsFile = app.vault.getAbstractFileByPath('08-科技树系统/01-OB_LifeOS_Build/00-通用模块库/echarts.js');
  if (echartsFile) {
    app.vault.read(echartsFile).then(content => {
      const script = document.createElement('script');
      script.textContent = content;
      document.head.appendChild(script);
      
      // 设置ECharts全局配置以优化事件处理
      if (window.echarts) {
        echarts.registerPostInit(function(chart) {
          // 尝试优化图表的事件处理，减少非被动事件监听器的使用
          chart.getZr().handler.__mouseup = function(event) {
            // 重写鼠标事件处理，避免阻塞滚动
            return true;
          };
        });
      }
      
      createExamTypeChart();
    });
  } else {
    console.error('ECharts文件未找到');
  }
} else {
  // 如果ECharts已加载，也设置全局配置
  echarts.registerPostInit(function(chart) {
    // 尝试优化图表的事件处理，减少非被动事件监听器的使用
    chart.getZr().handler.__mouseup = function(event) {
      // 重写鼠标事件处理，避免阻塞滚动
      return true;
    };
  });
  
  createExamTypeChart();
}

function createExamTypeChart() {
  // 获取原始数据
  const originalFile = app.vault.getAbstractFileByPath("07-项目系统/01-公务员考试/01-行测/01-刷题复盘/06-刷题记录/逻辑填空记录.md");
  
  if (!originalFile) {
    dv.paragraph("错误：无法找到原始数据文件");
    return;
  }
  
  app.vault.read(originalFile).then(content => {
    // 解析数据
    const records = content.split("\n").filter(line => line.includes("【") && line.includes("】"));
    
    const data = records.map(record => {
      const match = record.match(/【(\d{4})(\d{2})(\d{2})-\d{2}-(.+?)】(\d+)分钟，完成(\d+)道题，答对(\d+)道，答错(\d+)道.*正确率([\d.]+)%/);
      if (match) {
        return {
          date: `${match[1]}-${match[2]}-${match[3]}`,
          examType: match[4],
          time: parseInt(match[5]),
          total: parseInt(match[6]),
          correct: parseInt(match[7]),
          wrong: parseInt(match[8]),
          accuracy: parseFloat(match[9])
        };
      }
      return null;
    }).filter(item => item !== null);

    // 简化：统一考试类型分类函数
    function getTypeKey(type) {
      if (!type) return '其他';
      if (type.includes("国考")) return "国考";
      const provinceMatch = type.match(/(江苏|湖北|浙江|广东|山东|河南|四川|湖南|河北|福建|安徽|江西|山西|辽宁|吉林|黑龙江|陕西|甘肃|青海|新疆|西藏|内蒙古|广西|宁夏|海南|贵州|云南|重庆|天津|上海|北京)/);
      return provinceMatch ? provinceMatch[1] : '其他';
    }
    
    // 为每个考试类型分配独立颜色（与图2保持一致）
    const colorPalette = [
      '#5470c6', '#91cc75', '#fac858', '#ee6666', '#73c0de',
      '#3ba272', '#fc8452', '#9a60b4', '#ea7ccc', '#8dd3c7',
      '#bebada', '#fb8072', '#80b1d3', '#fdb462', '#b3de69'
    ];
    
    // 获取所有考试类型并分配颜色
    const allExamTypes = [...new Set(data.map(item => {
      const typeKey = getTypeKey(item.examType);
      return typeKey;
    }))];
    
    const typeColors = {};
    allExamTypes.forEach((type, index) => {
      typeColors[type] = colorPalette[index % colorPalette.length];
    });
    
    // 生成“考试类型分布图”聚合数据：每考试类型按题目年份的正确率曲线
    const typeGroups = {};
    data.forEach(item => {
      const typeKey = getTypeKey(item.examType);
      const year = parseInt(item.examType.match(/(\d{4})年/)?.[1] || '0');
      if (!typeGroups[typeKey]) typeGroups[typeKey] = { total: 0, correct: 0, records: [], byYear: {} };
      const g = typeGroups[typeKey];
      g.total += item.total;
      g.correct += item.correct;
      g.records.push(item);
      if (!g.byYear[year]) g.byYear[year] = { total: 0, correct: 0 };
      g.byYear[year].total += item.total;
      g.byYear[year].correct += item.correct;
    });

    // 为图例与平均值计算准备 chartData
    const chartData = Object.keys(typeGroups).map(t => {
      const g = typeGroups[t];
      const accuracy = +((g.correct / g.total) * 100).toFixed(1);
      return { examType: t, accuracy, records: g.records.length, detailRecords: g.records };
    });

    // 使用工具库统一容器与响应式设置
    window.EChartsKit.applyDvContainerDefaults(dv);
    const container = window.EChartsKit.initContainer(dv, 300, 400);
    const isMobile = window.EChartsKit.isMobile();
    
    // 收集所有题目年份并排序
    const allExamYears = new Set();
    chartData.forEach(category => {
      category.detailRecords.forEach(record => {
        const examYear = parseInt(record.examType.match(/(\d{4})年/)?.[1] || "2025");
        allExamYears.add(examYear);
      });
    });
    
    // 排序年份并转换为字符串数组
    const sortedYears = Array.from(allExamYears).sort((a, b) => a - b);
    const yearCategories = sortedYears.map(year => year.toString());
    
    // 准备折线图数据
    const lineSeriesData = [];
    const averageLineData = [];
    
    chartData.forEach((category, categoryIndex) => {
      if (category.detailRecords.length > 0) { // 为所有有记录的类型创建折线
        // 按题目年份排序（从examType中提取年份）
        const sortedRecords = [...category.detailRecords].sort((a, b) => {
          // 从examType中提取年份，如"2018年国考逻辑填空"中的2018
          const yearA = parseInt(a.examType.match(/(\d{4})年/)?.[1] || "2025");
          const yearB = parseInt(b.examType.match(/(\d{4})年/)?.[1] || "2025");
          return yearA - yearB;
        });
        
        // 创建时间序列数据（按年份聚合后求正确率）
        const byYear = {};
        category.detailRecords.forEach(r => {
          const y = parseInt(r.examType.match(/(\d{4})年/)?.[1] || '2025');
          if (!byYear[y]) byYear[y] = { total: 0, correct: 0 };
          byYear[y].total += r.total;
          byYear[y].correct += r.correct;
        });
        const timeSeriesData = sortedYears.map(year => {
          const g = byYear[year];
          return g ? +((g.correct / g.total) * 100).toFixed(1) : null;
        });
        
        // 添加折线图数据
        lineSeriesData.push({
          name: category.examType,
          type: 'line',
          data: timeSeriesData,
          lineStyle: {
            width: 2,
            color: typeColors[category.examType]
          },
          itemStyle: {
            color: typeColors[category.examType],
            borderColor: '#fff',
            borderWidth: 1
          },
          symbol: 'circle',
          symbolSize: 6,
          smooth: true, // 平滑曲线
          connectNulls: true // 连接null值点，确保不连续年份的数据点被连接
        });
        
        // 添加平均值虚线数据 - 只需要一条水平线，不需要每个点都有值
        const avgAccuracy = parseFloat(category.accuracy);
        
        averageLineData.push({
          name: category.examType + '平均值',
          type: 'line',
          data: yearCategories.map(() => avgAccuracy), // 使用整轴水平虚线
          lineStyle: {
            width: 1.5,
            color: typeColors[category.examType],
            type: 'dashed',
            opacity: 0.8
          },
          itemStyle: { opacity: 0 },
          symbol: 'none',
          smooth: false,
          silent: false,
          z: 1,
          connectNulls: true,
          // 末端常显标签：确保用户不悬停也能看到均值
          label: {
            show: true,
            position: 'endTop',
            formatter: (params) => {
              const examTypeName = params.seriesName.replace('平均值', '');
              return `${examTypeName}: ${Number(params.value).toFixed(1)}%`;
            },
            color: typeColors[category.examType],
            fontSize: 11,
            padding: [-5, 4]  // 负值使文字更贴近虚线
          },
          // 在最后一个坐标点增加一个不可见的标记用于承载标签，避免端点被裁剪导致不显示
          markPoint: {
            symbol: 'circle',
            symbolSize: 1,
            itemStyle: { color: 'transparent' },
            label: {
              show: true,
              position: 'top',
              formatter: () => {
                return `${category.examType}: ${avgAccuracy.toFixed(1)}%`;
              },
              color: typeColors[category.examType],
              fontSize: 11,
              padding: [-5, 4]  // 负值使文字更贴近虚线
            },
            data: [{ coord: [yearCategories[yearCategories.length - 1], avgAccuracy] }]
          },
          emphasis: {
            lineStyle: { width: 2.5, opacity: 0.8 }
          }
        });
      }
    });
    
    // 说明 → 图例 → 轴名 → 图（在容器上方插入说明）
    window.EChartsKit.insertExplanation(dv, '展示不同考试类型按年份的正确率趋势；虚线代表该类型均值');
    // 说明 → 图例 → 轴名 → 图（在容器上方插入说明）
    window.EChartsKit.insertExplanation(dv, '展示每次练习的平均每题用时与正确率，按考试类型着色');
    // 插入到文档中
    dv.container.appendChild(container);
    
    // 使用setTimeout确保DOM已经渲染完成后再初始化图表
    setTimeout(() => {
      // 渲染图表 - 使用配置优化事件处理
      const myChart = echarts.init(container, null, {
        // 配置渲染器，可能有助于减少非被动事件监听器
        renderer: 'canvas',
        // 启用渐进式渲染
        progressive: 200,
        // 配置阈值，减少事件处理频率
        progressiveThreshold: 1000
      });
      // 将myChart绑定到容器上，以便事件处理函数可以访问
      container.myChart = myChart;
      // 计算Top5类型，并设置图例默认选中，仅显示Top5；平均值虚线不进入图例
      const sortedByRecords = chartData.filter(item => item.detailRecords.length > 0)
        .sort((a, b) => b.records - a.records);
      const topExamTypes = sortedByRecords.slice(0, 5).map(item => item.examType);
      const legendSelected = {};
      chartData.filter(item => item.detailRecords.length > 0).forEach(item => {
        legendSelected[item.examType] = topExamTypes.includes(item.examType);
      });
      
      const legendTop = 38;
      const option = {
        tooltip: {
          trigger: 'item',
          backgroundColor: 'rgba(255, 255, 255, 0.95)',
          borderColor: '#ccc',
          borderWidth: 1,
          textStyle: { color: '#333', fontSize: 14 },
          extraCssText: 'box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); border-radius: 6px; padding: 12px; max-width: none;',
          formatter: function(params) {
            if (params.seriesName.includes('平均值')) {
              const examTypeName = params.seriesName.replace('平均值', '');
              const category = chartData.find(item => item.examType === examTypeName);
              const accuracy = params.value;
              const typeColor = typeColors[examTypeName];
              return `<div style="min-width: 180px; max-width: 300px; font-size: 14px; line-height: 1.6; word-wrap: break-word; white-space: normal;">
                <div style="margin-bottom: 6px; font-weight: bold;">${examTypeName}</div>
                <div>正确率: <span style="font-weight: bold; display: inline-block; min-width: 80px; font-size: 16px; color: ${typeColor};">${Number(accuracy).toFixed(1)}%</span></div>
                <div>记录次数: <span style="display: inline-block; min-width: 60px;">${category.records}次</span></div>
              </div>`;
            } else {
              const category = chartData.find(item => item.examType === params.seriesName);
              const yearIndex = params.dataIndex;
              const examYear = sortedYears[yearIndex];
              const accuracy = params.value;
              const record = category.detailRecords.find(r => {
                const recordExamYear = parseInt(r.examType.match(/(\d{4})年/)?.[1] || '2025');
                return recordExamYear === examYear;
              });
              const typeColor = typeColors[params.seriesName];
              return `<div style="min-width: 180px; max-width: 300px; font-size: 14px; line-height: 1.6; word-wrap: break-word; white-space: normal;">
                <div style="margin-bottom: 6px; font-weight: bold;">${params.seriesName}</div>
                <div>题目年份: <span style="display: inline-block; min-width: 60px;">${examYear}</span></div>
                <div>正确率: <span style="font-weight: bold; display: inline-block; min-width: 80px; font-size: 16px; color: ${typeColor};">${accuracy}%</span></div>
                ${record ? `<div>记录日期: <span style="display: inline-block; min-width: 80px;">${record.date}</span></div>` : ''}
              </div>`;
            }
          }
        },
        legend: Object.assign(window.EChartsKit.legend(legendTop, 'center', 12), {
          type: 'scroll',
          data: [
            ...chartData.filter(item => item.detailRecords.length > 0).map(item => item.examType),
            ...chartData.filter(item => item.detailRecords.length > 0).map(item => item.examType + '平均值')
          ],
          selected: {
            ...legendSelected,
            ...Object.fromEntries(
              chartData.filter(item => item.detailRecords.length > 0)
                .filter(item => topExamTypes.includes(item.examType))
                .map(item => [item.examType + '平均值', true])
            )
          },
          // 图例项统一样式
          textStyle: { fontSize: 12, lineHeight: 16 },
          itemWidth: 18,
          itemHeight: 10
        }),
        grid: window.EChartsKit.grid({ top: window.EChartsKit.computeGridTop(legendTop), left: window.EChartsKit.computeGridLeftForYAxis(3), bottom: 60, containLabel: true }),
        xAxis: { 
          type: 'category', 
          name: '题目年份(年)', 
          nameLocation: 'middle',
          nameGap: 30,
          data: yearCategories, 
          axisLabel: { formatter: '{value}' } 
        },
        yAxis: { 
          type: 'value', 
          name: '正确率(%)', 
          nameLocation: 'end',
          nameGap: window.EChartsKit.nameGap(),
          nameTextStyle: {
            fontSize: 14,
            fontWeight: 'normal'
          },
          min: 40, 
          max: 100, 
          axisLabel: { formatter: '{value}' } 
        },
        series: [
          // 仅为Top5类型渲染平均值虚线，避免干扰
          ...averageLineData.filter(s => topExamTypes.includes(s.name.replace('平均值', ''))),
          ...lineSeriesData
        ]
      };
      
      myChart.setOption(option);
      
      // 添加鼠标悬停联动功能：当悬停在虚线上时，高亮对应的记录正确率数据点
      container.myChart.on('mouseover', function(params) {
        if (params.seriesName.includes('平均值')) {
          const examTypeName = params.seriesName.replace('平均值', '');
          // 高亮对应的记录正确率数据点
          container.myChart.dispatchAction({
            type: 'highlight',
            seriesName: examTypeName
          });
        }
      });
      
      // 添加鼠标移出功能：当鼠标移出虚线时，取消高亮对应的记录正确率数据点
      container.myChart.on('mouseout', function(params) {
        if (params.seriesName.includes('平均值')) {
          const examTypeName = params.seriesName.replace('平均值', '');
          // 取消高亮对应的记录正确率数据点
          container.myChart.dispatchAction({
            type: 'downplay',
            seriesName: examTypeName
          });
        }
      });
      
      // 添加图例联动功能：当点击图例时，同时控制日记录和对应的平均值线
      container.myChart.on('legendselectchanged', function(params) {
        const selected = params.selected;
        
        // 遍历所有选中的考试类型
        Object.keys(selected).forEach(examType => {
          // 查找对应的平均值线名称
          const averageLineName = examType + '平均值';
          
          // 同步平均值线的显示状态
          if (selected[examType]) {
            // 如果日记录被选中，确保平均值线也被选中
            container.myChart.dispatchAction({
              type: 'legendSelect',
              name: averageLineName
            });
          } else {
            // 如果日记录被取消选中，也取消选中平均值线
            container.myChart.dispatchAction({
              type: 'legendUnSelect',
              name: averageLineName
            });
          }
        });
      });
      
      // 响应式调整（统一工具库）
      window.EChartsKit.addPassiveResize(dv, container, isMobile => isMobile ? 300 : 400);
    }, 100); // 给DOM一点时间来更新
  }).catch(err => {
    dv.paragraph(`读取文件错误: ${err.message}`);
  });
}
```

### 题量与用时关系散点图

```dataviewjs
// 确保ECharts已加载
if (!window.echarts) {
  const echartsFile = app.vault.getAbstractFileByPath('08-科技树系统/01-OB_LifeOS_Build/00-通用模块库/echarts.js');
  if (echartsFile) {
    app.vault.read(echartsFile).then(content => {
      const script = document.createElement('script');
      script.textContent = content;
      document.head.appendChild(script);
      
      // 设置ECharts全局配置，优化事件处理
      echarts.registerPostInit(function(chart) {
        // 重写鼠标事件处理函数，避免阻塞滚动
        if (chart.getZr() && chart.getZr().handler) {
          const originalMouseup = chart.getZr().handler.__mouseup;
          chart.getZr().handler.__mouseup = function(event) {
            // 使用requestAnimationFrame确保不阻塞主线程
            requestAnimationFrame(function() {
              if (originalMouseup) {
                originalMouseup.call(this, event);
              }
            });
          };
        }
      });
      
      createScatterChart();
    });
  } else {
    console.error('ECharts文件未找到');
  }
} else {
  // 设置ECharts全局配置，优化事件处理
  echarts.registerPostInit(function(chart) {
    // 重写鼠标事件处理函数，避免阻塞滚动
    if (chart.getZr() && chart.getZr().handler) {
      const originalMouseup = chart.getZr().handler.__mouseup;
      chart.getZr().handler.__mouseup = function(event) {
        // 使用requestAnimationFrame确保不阻塞主线程
        requestAnimationFrame(function() {
          if (originalMouseup) {
            originalMouseup.call(this, event);
          }
        });
      };
    }
  });
  
  createScatterChart();
}

function createScatterChart() {
  // 获取原始数据
  const originalFile = app.vault.getAbstractFileByPath("07-项目系统/01-公务员考试/01-行测/01-刷题复盘/06-刷题记录/逻辑填空记录.md");
  
  if (!originalFile) {
    dv.paragraph("错误：无法找到原始数据文件");
    return;
  }
  
  app.vault.read(originalFile).then(content => {
    // 解析数据
    const records = content.split("\n").filter(line => line.includes("【") && line.includes("】"));
    
    const data = records.map(record => {
      const match = record.match(/【(\d{4})(\d{2})(\d{2})-\d{2}-(.+?)】(\d+)分钟，完成(\d+)道题，答对(\d+)道，答错(\d+)道.*正确率([\d.]+)%/);
      if (match) {
        return {
          date: `${match[1]}-${match[2]}-${match[3]}`,
          examType: match[4],
          time: parseInt(match[5]),
          total: parseInt(match[6]),
          correct: parseInt(match[7]),
          wrong: parseInt(match[8]),
          accuracy: parseFloat(match[9])
        };
      }
      return null;
    }).filter(item => item !== null);

    // 简化：统一考试类型分类函数
    function getTypeKey(type) {
      if (!type) return '其他';
      if (type.includes("国考")) return "国考";
      const provinceMatch = type.match(/(江苏|湖北|浙江|广东|山东|河南|四川|湖南|河北|福建|安徽|江西|山西|辽宁|吉林|黑龙江|陕西|甘肃|青海|新疆|西藏|内蒙古|广西|宁夏|海南|贵州|云南|重庆|天津|上海|北京)/);
      return provinceMatch ? provinceMatch[1] : '其他';
    }
    
    // 为每个考试类型分配独立颜色（与图2保持一致）
    const colorPalette = [
      '#5470c6', '#91cc75', '#fac858', '#ee6666', '#73c0de',
      '#3ba272', '#fc8452', '#9a60b4', '#ea7ccc', '#8dd3c7',
      '#bebada', '#fb8072', '#80b1d3', '#fdb462', '#b3de69'
    ];
    
    // 获取所有考试类型并分配颜色
    const allExamTypes = [...new Set(data.map(item => {
      const typeKey = getTypeKey(item.examType);
      return typeKey;
    }))];
    
    const typeColors = {};
    allExamTypes.forEach((type, index) => {
      typeColors[type] = colorPalette[index % colorPalette.length];
    });
    
    // 生成每次记录的散点图数据
    const scatterData = data.map(item => {
      // 计算平均每题用时
      const avgTimePerQuestion = item.time / item.total;
      
      const typeKey = getTypeKey(item.examType);
      
      return {
        value: [avgTimePerQuestion, item.accuracy],
        type: typeKey,
        originalType: item.examType,
        date: item.date,
        total: item.total,
        time: item.time,
        correct: item.correct,
        wrong: item.wrong
      };
    });
    
    // 统计各类型出现次数，默认仅选中Top6；并优化点样式
    const typeCounts = {};
    scatterData.forEach(d => { typeCounts[d.type] = (typeCounts[d.type] || 0) + 1; });
    const topTypes = Object.entries(typeCounts).sort((a, b) => b[1] - a[1]).slice(0, 6).map(([t]) => t);
    const legendSelectedScatter = {};
    allExamTypes.forEach(t => { legendSelectedScatter[t] = topTypes.includes(t); });

    // 按考试类型分组数据
    const scatterSeries = allExamTypes.map(type => {
      return {
        name: type,
        type: 'scatter',
        data: scatterData.filter(item => item.type === type),
        symbolSize: 9,
        itemStyle: { color: typeColors[type] },
        emphasis: { focus: 'series', itemStyle: { borderColor: '#fff', borderWidth: 1 } }
      };
    });
    
    // 应用统一容器样式并创建图表容器（复用工具库）
    window.EChartsKit.applyDvContainerDefaults(dv);
    const container = window.EChartsKit.initContainer(dv, 300, 400);
    const isMobile = window.EChartsKit.isMobile();
    
    // 计算数据中的最小和最大平均每题用时
    const timePerQuestionValues = scatterData.map(item => item.value[0]);
    const minTimePerQuestion = Math.min(...timePerQuestionValues);
    const maxTimePerQuestion = Math.max(...timePerQuestionValues);
    
    // 计算数据中的最小和最大正确率
    const accuracyValues = scatterData.map(item => item.value[1]);
    const minAccuracy = Math.min(...accuracyValues);
    const maxAccuracy = Math.max(...accuracyValues);
    
    // 插入到文档中
    dv.container.appendChild(container);
    
    // 使用setTimeout确保DOM已经渲染完成后再初始化图表
    setTimeout(() => {
      // 渲染图表 - 使用配置优化事件处理
      const myChart = echarts.init(container, null, {
        // 配置渲染器，可能有助于减少非被动事件监听器
        renderer: 'canvas',
        // 启用渐进式渲染
        progressive: 200,
        // 配置阈值，减少事件处理频率
        progressiveThreshold: 1000
      });
      // 将myChart绑定到容器上，以便事件处理函数可以访问
      container.myChart = myChart;
      const legendTop = 38;
      const option = {
        tooltip: {
          backgroundColor: 'rgba(255, 255, 255, 0.95)',
          borderColor: '#ccc',
          borderWidth: 1,
          textStyle: {
            color: '#333',
            fontSize: 14
          },
          extraCssText: 'box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); border-radius: 6px; padding: 12px; max-width: none;',
          formatter: function(params) {
            const data = params.data;
            // 增加容器宽度和数值宽度，确保数字完整显示
            return `<div style="min-width: 220px; max-width: 350px; font-size: 14px; line-height: 1.6; word-wrap: break-word; white-space: normal;">
              <div style="margin-bottom: 8px; font-weight: bold; font-size: 15px; color: #333;">${data.originalType}</div>
              <div style="margin-bottom: 4px;">日期: <span style="display: inline-block; min-width: 90px; color: #555;">${data.date}</span></div>
              <div style="margin-bottom: 4px;">题量: <span style="display: inline-block; min-width: 60px; color: #555;">${data.total}题</span></div>
              <div style="margin-bottom: 4px;">用时: <span style="display: inline-block; min-width: 70px; color: #555;">${data.time}分钟</span></div>
              <div style="margin-bottom: 4px;">平均每题用时: <span style="display: inline-block; min-width: 80px; font-weight: bold; color: #3498db;">${data.value[0].toFixed(2)}分钟</span></div>
              <div>正确率: <span style="display: inline-block; min-width: 60px; font-weight: bold; color: #e74c3c;">${data.value[1]}%</span></div>
            </div>`;
          }
        },
        legend: Object.assign(window.EChartsKit.legend(legendTop, 'center', 12), {
          type: 'scroll',
          data: allExamTypes,
          selected: legendSelectedScatter,
          textStyle: { fontSize: 12, lineHeight: 16 },
          itemWidth: 18,
          itemHeight: 10
        }),
        grid: window.EChartsKit.grid({ top: window.EChartsKit.computeGridTop(legendTop), left: window.EChartsKit.computeGridLeftForYAxis(3), bottom: 60, containLabel: true }),
        xAxis: {
          type: 'value',
          name: '平均每题用时(分钟)',
          nameLocation: 'middle',
          nameGap: 30,
          min: Math.floor(minTimePerQuestion * 0.9),
          max: Math.ceil(maxTimePerQuestion * 1.1),
          axisLine: { show: false },
          axisTick: { show: false },
          splitLine: { show: true, lineStyle: { color: '#eee' } }
        },
        yAxis: {
          type: 'value',
          name: '正确率(%)',
          nameLocation: 'end',
          nameGap: window.EChartsKit.nameGap(),
          nameTextStyle: {
            fontSize: 14,
            fontWeight: 'normal'
          },
          min: Math.floor(minAccuracy * 0.9),
          max: Math.ceil(maxAccuracy * 1.1),
          axisLine: { show: false },
          axisTick: { show: false },
          axisLabel: { formatter: '{value}' },
          splitLine: { show: true, lineStyle: { color: '#eee' } }
        },
        series: scatterSeries
      };
      
      myChart.setOption(option);
      
      // 响应式调整（统一工具库）
      window.EChartsKit.addPassiveResize(dv, container, isMobile => isMobile ? 300 : 400);
    }, 100); // 给DOM一点时间来更新
  }).catch(err => {
    dv.paragraph(`读取文件错误: ${err.message}`);
  });
}
```
